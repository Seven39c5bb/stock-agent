股票信息查询智能体（Stock Intel Agent）产品设计文档
一、 项目概述
本项目旨在打造一个基于大语言模型（LLM）的智能股票分析网页。用户通过输入股票名称或代码，系统将自动采集实时数据、历史趋势及财务信息，并通过智能体（Agent）进行深度分析，输出结构化的投资参考建议。

二、 核心功能模块
1. 输入模块（Input Layer）
功能描述：提供简洁的搜索入口。
关键要素：
智能匹配：支持输入中文名称（如：贵州茅台）或股票代码（如：600519/AAPL）。
校验机制：前端需对输入格式进行初步过滤，后端接入证券代码库进行实时模糊匹配。
2. 数据采集模块（Data Engine）
功能描述：为智能体提供分析所需的“原材料”。
技术选型建议：对接金融 API（如 AkShare, Tushare 或 Yahoo Finance）。
采集维度：
基础信息：公司名称、所属行业、主营业务。
行情数据：最新交易日收盘价、涨跌幅、成交量。
历史数据：近 30 天的历史价格序列（用于走势分析）。
财务指标：市盈率（PE）、市净率（PB）、ROE 等关键指标。
3. 智能分析模块（Agent Reasoning）
核心逻辑：采用 LLM + Tool Use (Function Calling) 模式。
分析维度（对应图中五个部分）：
公司基本概况：提取业务核心亮点及行业地位。
最新价格：格式化展示最新收盘价及其市场含义。
走势分析：基于近一月数据，计算斜率、波动率，识别多空趋势。
长期价值评估：结合财务指标与行业前景，评估当前估值水平。
模拟投资策略：针对 10 万元本金，给出具体的仓位管理建议（如：分批建仓、止损点位设置）。
三、 输出规范（Output Template）
网页输出端需按照以下结构化展示（采用 Markdown 渲染以保证排版美观）：

模块	内容要求	展现形式
一、股票公司基本概况	业务范围、行业地位、近期重大事件	文本摘要
二、最新价格	展示“上一交易日收盘价”，标注涨跌幅	醒目数值 + 趋势标签
三、近一月走势分析	分析支撑位/压力位，总结走势特征（如：震荡上行）	深度文本分析
四、长期投资价值	基于估值百分位、盈利能力的综合判断	逻辑论述
五、10万模拟投资策略	仓位比例（如 3:4:3）、预估持有周期、风险提示	结构化方案
四、 技术架构建议
前端：React.js 或 Vue.js + Tailwind CSS（保证响应式布局，适配移动端）。
后端：Python (FastAPI / Flask) —— Python 在金融数据处理和 LLM 调用上有天然优势。
智能体框架：LangChain 或 LangGraph，用于编排数据获取和分析逻辑。
模型选择：建议使用 GPT-4o 或 DeepSeek-V3 等推理能力强的模型，以确保投资建议的逻辑严密性。
五、 项目关键风险与对策
数据延迟：在页面显著位置标注“数据非实时，仅供参考”。
合规性：增加免责声明，明确表示智能体输出内容不构成法律意义上的投资建议。
幻觉问题：通过 RAG（检索增强生成）技术，强制模型基于抓取的真实数据进行回答，严禁凭空编造股价